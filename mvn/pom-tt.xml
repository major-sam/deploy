<project>
	<modelVersion>4.0.0</modelVersion>
	<properties>
			<deploy.branch>master</deploy.branch>
			<deploy.version>LATEST</deploy.version>
			<deploy.dir>c:/tmp</deploy.dir>
	</properties>
	<groupId>Mvn</groupId>
	<artifactId>MvnDeploy</artifactId>
	<version>1.0.1</version>
	<packaging>pom</packaging>
	<repositories>
		<repository>
			<id>maven-group</id>
			<url>http://nexus:8081/repository/maven-group/</url>
		</repository>
	</repositories>
	<dependencies>
		<dependency>
			<groupId>TradingTool</groupId>
			<artifactId>${deploy.branch}</artifactId>
			<version>${deploy.version}</version>
			<classifier>Release</classifier>
			<type>zip</type>
		</dependency>
	</dependencies>
	<build>
	 <plugins>
		<plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>versions-maven-plugin</artifactId>
            <version>2.7</version>
            <configuration>
                <excludes>
                    <exclude>org.apache.commons:commons-collections4</exclude>
                </excludes>
            </configuration>
       </plugin>
       <plugin>
         <groupId>org.apache.maven.plugins</groupId>
         <artifactId>maven-dependency-plugin</artifactId>
         <version>3.2.0</version>
	   <configuration>
          <artifactItems>
            <artifactItem>
              <groupId>TradingTool</groupId>
              <artifactId>${deploy.branch}</artifactId>
			<version>${testng.version}</version>
			<classifier>Release</classifier>
			<type>zip</type>
              <overWrite>true</overWrite>
              <outputDirectory>${deploy.dir}</outputDirectory>
              <destFileName>test</destFileName>
            </artifactItem>
          </artifactItems>
          <!-- other configurations here -->
        </configuration>
       </plugin>
     </plugins>
	 </build>
</project>